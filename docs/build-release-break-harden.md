# Build, Release, Break, Harden

Build the local FastAPI application, release only hardened mode, break it with isolated baseline demos and attack suites, harden deterministic controls, then rerun identical attacks and regression tests. Commands and observed outputs are recorded under `docs/evidence/`.
