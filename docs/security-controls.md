# Security controls

Implemented controls include hashed passwords, expiring sessions, deterministic object authorization, strict request and model schemas, untrusted-content separation, PII redaction, read-only tool allowlisting, high-risk review gates, privacy-minimized audit events, rate limiting, security headers, and generic errors. Each control has tests or is explicitly marked limited in the acceptance report.
